import CoreData
import CoreDataEvolution
import Foundation

@objc(Item)
@PersistentModel(
  generateInit: true,
  relationshipSetterPolicy: .plain,
  relationshipCountPolicy: .none
)
final class Item: NSManagedObject {
  @Attribute(originalName: "timestamp")
  var date: Date?

  var title: String = ""

  @Ignore
  var transientCache: [String: Int] = [:]

  @Inverse(Tag.self, "items")
  var tags: Set<Tag>

  @Inverse(Tag.self, "orderedItems")
  var orderedTags: [Tag]
  var category: Category?
}
